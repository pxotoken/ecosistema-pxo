# Server
PORT=3006
HOST=0.0.0.0
NODE_ENV=development

# CORS — who can hit this service directly
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:3003

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Manual KYC review — comma-separated wallet addresses with admin rights.
# Replace with proper RBAC (role claims in JWT) when ready.
KYC_ADMIN_WALLETS=
